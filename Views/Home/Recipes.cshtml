@{
    ViewData["Title"] = "Рецепты";
}
<section id="recipes" class="recipes-preview-section">
    <div class="container">
        <h2>Популярные рецепты</h2>
        <div style="margin-bottom:20px;">
            <a asp-action="Index" class="btn btn-outline-primary">Все</a>
            @if (ViewBag.Categories != null)
            {
                foreach (var cat in ViewBag.Categories)
                {
                    <a asp-action="Index" asp-route-categoryId="@cat.Id"
                       class="btn btn-outline-secondary" style="margin-left:5px;">
                        @cat.Name
                    </a>
                }
            }

            @*  @foreach (var cat in ViewBag.Categories)
            {
            <a asp-action="Index" asp-route-categoryId="@cat.Id"
            class="btn btn-outline-secondary" style="margin-left:5px;">
            @cat.Name
            </a>
            } *@
        </div>
        <div class="recipes-grid">
            
            <div class="recipe-preview-card">
                <img src="/images/recipes/borsh.jpg" alt="Борщ">
                <h3>Борщ домашний</h3>
                <p>Традиционный ароматный борщ с говядиной и свежими овощами.</p>
                <a href="/Recipes" class="btn-more">Перейти к рецептам</a>
            </div>

            <div class="recipe-preview-card">
                <img src="/images/recipes/olivie.jpg" alt="Оливье">
                <h3>Салат Оливье</h3>
                <p>Любимый праздничный салат в классическом исполнении.</p>
                <a href="/Recipes" class="btn-more">Перейти к рецептам</a>
            </div>

            <div class="recipe-preview-card">
                <img src="/images/recipes/plov.jpg" alt="Плов">
                <h3>Узбекский плов</h3>
                <p>Ароматный и рассыпчатый плов, приготовленный как в казане.</p>
                <a href="/Recipes" class="btn-more">Перейти к рецептам</a>
            </div>

        </div>
    </div>
</section>

